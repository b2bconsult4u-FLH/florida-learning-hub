(()=>{const button=document.querySelector(".back-to-top");if(!button)return;const reduced=window.matchMedia("(prefers-reduced-motion: reduce)");const update=()=>button.classList.toggle("is-visible",window.scrollY>320);button.addEventListener("click",()=>window.scrollTo({top:0,behavior:reduced.matches?"auto":"smooth"}));window.addEventListener("scroll",update,{passive:true});update();})();
